const iconMenu=document.querySelector(".menu__icon"),menuBody=document.querySelector(".header__menu");iconMenu&&iconMenu.addEventListener("click",(function(e){document.body.classList.toggle("--lock"),iconMenu.classList.toggle("--active"),menuBody.classList.toggle("--active")}));const menuLinks=document.querySelectorAll(".menu__link");menuLinks.forEach((e=>{e.addEventListener("click",(()=>{iconMenu.classList.contains("--active")&&(document.body.classList.remove("--lock"),iconMenu.classList.remove("--active"),menuBody.classList.remove("--active"))}))}));